apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: concourse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  selector:
    matchLabels:
      type: concourse
